node {
   stage('Clone Repository') { // for display purposes
      // Get some code from a GitHub repository
      git 'https://github.com/Tharwat96/jenkins-test.git'

   }
   stage('Great Script Running') {
      sh 'mkdir test2'
      sh 'mkdir test3'
   }
   stage('Results') {
      sh 'ls /'
      sh 'mkdir /test'
      echo 'this is branch3'
   }
}
